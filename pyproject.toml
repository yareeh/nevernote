[project]
name = "my-joplin"
version = "0.1.0"
description = "Evernote -> Joplin migration and self-hosted Joplin Server"
requires-python = ">=3.12"
dependencies = [
    "evernote-backup==1.14.0",
]

[tool.uv]
package = false
