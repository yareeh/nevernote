# Copy to .env to override the defaults.

# Directory with the .enex files to serve (one per notebook; stacks are
# subdirectories). `make evernote-export` writes to data/enex.
ENEX_DIR=./data/enex

# Port on this box for the viewer.
VIEWER_PORT=8765
